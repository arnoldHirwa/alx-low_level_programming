#include "main.h"

/**
  * mul - Multiplies two integers
  * @k: The first number to multiply
  * @l: The second number to multiply with first number
  *
  * Return: The result of multiplication between k param and l param
  */
int mul(int k, int l)
{
	return (k * l);
}

